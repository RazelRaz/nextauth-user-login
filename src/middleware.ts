// export { default } from "next-auth/middleware"

// export const config = { matcher: ["/extra", "/dashboard"] }



export function middleware(){
    const config = { matcher: ["/extra", "/dashboard"] }
}
